# Proposal

Resolve OQ#5 (PRO-421) by locking the @carte/ai trial and licensing pattern in `PRD.md`. The update codifies the server-side license check at `license.carteplugin.dev`, 24-hour KV caching, outage-safe graceful degradation, Lemon Squeezy as the recommended billing provider, and the Worker + D1 license-service stack so restaurant operations never depend on a live licensing round-trip.
