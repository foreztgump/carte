## Analytical Lens

Treat LatePoint as the mission's reference implementation for reservation modeling rather than a product to emulate wholesale. The teardown will focus on how its PHP structure, availability pipeline, notification system, and admin surfaces solve booking problems today, then explicitly compare those choices to Carte's PRD—especially the read-time slot computation pattern—so downstream workers can adopt patterns selectively without copying implementation details.
