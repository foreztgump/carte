## Rationale

This feature applies an already-decided documentation correction: the Linear team key is `PRO`, so the repo guidance must stop advertising `CART-XXX` for day-to-day workflow strings. The safest design is a surgical update limited to `AGENTS.md`, preserving the planned `CART` project identifier note while replacing only the branch, commit, and `/work` examples that should follow the actual team key.
