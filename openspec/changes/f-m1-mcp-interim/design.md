# Design

This feature is a focused documentation correction driven by the mission's locked research conclusions. The implementation keeps changes narrow: clarify the AI-layer integration contract in `PRD.md`, align OQ#2 with the same wording, and capture upstream interest through a single Discussion #850 comment so later workers can reference a concrete interim pattern without re-researching MCP support.
