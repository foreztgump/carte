# Proposal

Lock the EmDash sandbox quotas in `PRD.md` and `AGENTS.md` using the mission's already-verified constraints. This change removes lingering hedge language, records the full sandbox envelope (CPU, subrequests, wall time, memory), and states that Cloudflare Free cannot host sandboxed plugins because Dynamic Workers are unavailable.
