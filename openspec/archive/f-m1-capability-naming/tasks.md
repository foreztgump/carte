# Tasks

- [ ] Update `PRD.md` capability references in `EmDash Architecture Constraints`, all five `Capability Manifests` examples, the AI layer note, and OQ#1 so only canonical capability names remain.
  - Acceptance: `read:content`, `write:content`, `read:media`, `write:media`, and `network:fetch` no longer appear in `PRD.md`.
- [ ] Update `AGENTS.md` capability guidance to the canonical names and cite the EmDash SKILL.md source of truth.
  - Acceptance: `AGENTS.md` contains only canonical capability names and no unresolved naming language.
- [ ] Verify with grep and update PRO-419 state.
  - Acceptance: grep for deprecated names across `PRD.md` and `AGENTS.md` returns no matches; PRO-419 is no longer In Progress.
