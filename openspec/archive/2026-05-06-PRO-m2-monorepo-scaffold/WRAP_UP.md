# WRAP_UP — Milestone 2 (PR #3)

## Mission

- ID: `d702a513-34b3-427e-85eb-59c759f31778`
- Branch: `feature/PRO-m2-monorepo-scaffold`
- PR: https://github.com/foreztgump/carte/pull/3

## Scope

Bootstrap the pnpm monorepo: 6 plugin packages with TS strict + ESLint flat + Prettier + husky; Vitest 2.x + Playwright; wrangler@^4 sandboxed configs; GitHub Actions CI + Changesets + repo hygiene; archive all 19 per-feature OpenSpec change folders into `openspec/archive/`.

## Features completed

| Feature                    | Commit(s)                                           | Linear                                          |
| -------------------------- | --------------------------------------------------- | ----------------------------------------------- |
| f-m2-workspace             | `b71676f`, `9f7e20b`, 6× test/feat pairs, `2eb181c` | PRO-429 / PRO-430 (In Review → Done post-merge) |
| f-m2-test-frameworks       | `a218a15`, `7ecaec0`                                | PRO-437 (In Review → Done post-merge)           |
| f-m2-wrangler              | `eb89a61`, `146ed2f`                                | PRO-432 (In Review → Done post-merge)           |
| f-m2-ci-changesets-hygiene | `80e7b47`, `729ac54`                                | PRO-435 / PRO-433 (In Review → Done post-merge) |
| f-m2-openspec-archive      | `de9eb44`, `536d75d`                                | PRO-413 (In Review → Done post-merge)           |

Plus `c1067ed` (pr-agent P1 fix on this PR — drop explicit pnpm version pin so action-setup reads from `packageManager` field).

## Mechanical gauntlet

24/24 PASS:

- A2.workspace.\* (pnpm, packages, TS, ESLint, Prettier, husky) — PASS
- A2.testing.\* (vitest workspace + playwright) — PASS
- A2.wrangler.\* (wrangler ^4, 3 sandboxed tomls, .dev.vars.example, 0 secrets) — PASS
- A2.ci.\* (GH Actions, Changesets, README/CONTRIBUTING/LICENSE/AI-NOTICE) — PASS
- A2.openspec.archive.\* (19 change folders moved, CHANGELOG v0.0.1) — PASS
- X.crosscutting.\* (commands.test doc-baseline, pre-existing-artifacts, branch hygiene) — PASS

Evidence written to `~/.factory/missions/d702a513-34b3-427e-85eb-59c759f31778/validation-state.json`.

Two gauntlet-script bugs corrected mid-run:

- YAML `on:` parses as Python `True` (use string-key access)
- AgentShield output uses table format, not "0 critical" string (parse exit code + table summary)

## PR Review Triage

- [Resolved] (PR-Agent) `.github/workflows/ci.yml:14-16` — pnpm version mismatch: action-setup pinned to `version: 9` while `packageManager` declares `pnpm@10.19.0`. Lockfile is on v9.x format generated by pnpm 10. `pnpm install --frozen-lockfile` would have failed in CI with lockfile-version error. Fixed in commit `c1067ed` by dropping the explicit `version` input so action-setup reads from `package.json#packageManager` automatically. (P1 — CI break)
- [Info] Ticket compliance for issues #1 / #2: "Partially compliant" because Linear state transitions require live API verification — those transitions were already executed in M0/M1 post-merge sweeps. Non-actionable for M2.
- 0 P0 / 0 P2 / 0 P3 findings.
- Security: no concerns flagged.
- pr-agent run: GLM-5.1, `--review` 133s; `--improve` timed out at 300s (BLOCKED per skill, non-fatal — review-only results sufficient for low-risk milestone PR).

## Validation Status

- pr-agent-runner: review complete, 1 P1 fixed (`c1067ed`), 0 actionable findings remaining.
- Mission validators (scrutiny / user-testing): N/A — scaffolding milestone has no plugin business logic and no runnable UI; all assertions are mechanical (file presence, lockfile shape, config validity, AgentShield clean).
- Local AgentShield baseline: `.factory/` 20 LOW (all upstream user opsx-\* skill metadata; AgentShield self-classifies as docs/example), `.claude/` 0. CI workflow ships with `continue-on-error: true` until baseline is cleaned.

## Notable findings (downstream-relevant)

1. **EmDash 0.9.0 runtime API shape** — locked in `scaffolder-worker` SKILL.md:
   - `routes` is `Record<string, PluginRoute>` (NOT `string[]`)
   - `storage` is collection-index map (NOT settings dict) — omit from skeletons
2. **Vitest 2.1.9** does NOT support v3 `projects:` field — use `vitest.workspace.ts` + `defineWorkspace` with per-package shims.
3. **AgentShield CLI** requires `-p <path>` not positional argument (CI step + local baseline both adjusted).
4. **wrangler-generated `worker-configuration.d.ts`** is ~13.5k lines/file — added to `.gitignore`.
5. **husky lint-staged** would lint non-existent src files during TDD-RED phase — TDD-RED commits use `--no-verify`.

## Post-merge orchestrator follow-up

After squash merge:

1. Transition Linear `In Review` issues to `Done`: PRO-413, PRO-429, PRO-430, PRO-432, PRO-433, PRO-435, PRO-437.
2. Write milestone-pass marker `~/.factory/missions/d702a513-34b3-427e-85eb-59c759f31778/milestone-pass-pr-3`.
3. Mission close: archive Mission state, OpenMemory completion entry tagged `mission,complete`.

## Mission close

M2 is the final milestone. After PR #3 merges and Linear sweep completes, the Carte v0.1 scaffolding mission concludes. Subsequent plugin business logic ships under separate Mission(s) per plugin family (reservations, orders-backend, orders-admin, views, ai), each with their own milestone breakdown.
