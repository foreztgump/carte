# Design

This feature is a documentation/configuration lock-in, not a product design change. The implementation is intentionally surgical: add one clearly labeled stack-pin section to `AGENTS.md` and replace the soft, outdated EmDash version guidance in `openspec/config.yaml` with the mission's locked `^0.9.0` decision plus the rationale for avoiding `emdash@1.0.0`.
