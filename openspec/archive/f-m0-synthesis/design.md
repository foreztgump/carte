# Design

This synthesis treats the three WP plugins as evidence sources for reusable restaurant-product patterns rather than implementation templates. The document highlights where cross-plugin convergence signals a strong operational need, then translates that need into Carte-native package boundaries and explicit adopt/adapt/reject decisions.
