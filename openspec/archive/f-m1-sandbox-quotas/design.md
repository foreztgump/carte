# Design

This is a documentation-only change that applies the orchestrator's locked research conclusions to the PRD and repository guidance. The design goal is consistency: every sandbox-related statement should describe the same hard runtime envelope and the same Cloudflare Free-plan limitation so future feature workers budget against one authoritative set of constraints.
