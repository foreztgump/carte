{
  "name": "@carte/ai",
  "version": "0.1.0",
  "private": true,
  "description": "Carte AI — paid native plugin (BYO LLM key, license-checked). EmDash native plugin.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "emdash": "^0.9.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.6"
  }
}
