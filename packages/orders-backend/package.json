{
  "name": "@carte/orders-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Carte orders backend — Stripe Checkout + webhook + refund routes. Sandboxed EmDash plugin.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "peerDependencies": {
    "@tender/sdk": "^0.1.0",
    "emdash": "^0.9.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.8.69",
    "@tender/sdk": "workspace:*",
    "typescript": "^5.7.2",
    "vitest": "^2.1.6"
  }
}
